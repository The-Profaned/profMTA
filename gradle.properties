# Default TitanLauncher sync repository. The net.titan.dev plugin resolves
# state.json to the current releases/<version> directory containing controller.exe.
titanClientRoot=%USERPROFILE%/.titanclient/repository
